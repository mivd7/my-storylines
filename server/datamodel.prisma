type Story {
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  content: String!
  additions: String
  published: Boolean
  author: User
}

type User {
  id: ID! @unique
  name: String
  email: String! @unique
  accessRole: AccessRole!
  stories: [Story!]!
}

type Addition {
  id: ID! @unique
  createdAt: DateTime!
  text: String!
  story: Story!
  writtenBy: User
}

enum AccessRole {
  USER
  ADMIN
}